以下是该脚本的详细使用说明文档：

# 多相机彩色图像采集脚本使用说明

## 概述

本脚本用于同时采集 1 台 RealSense 相机和 2 台 Orbbec 相机的彩色图像，并按照预定义的目录结构保存到数据集目录中。主要用于实验室环境下的容器质量检测数据集采集。

## 功能特点

- 支持 1 台 RealSense + 2 台 Orbbec 相机同时采集
- 自动识别 Orbbec 相机并分配角色（c1/c2）
- 按照容器类型、异常类型、拍摄点位等层级组织目录结构
- 支持预设的异常类型和小类分类体系
- 提供相机列表查看和类型列表查看功能

## 环境要求

### 依赖库
- Python 3.6+
- OpenCV (cv2)
- NumPy
- pyrealsense2
- pyorbbecsdk

### 硬件要求
- 1 台 RealSense 相机
- 2 台 Orbbec 相机（其中一台序列号为 `CL8K14100H4` 作为 c1）

## 安装配置

确保已安装所有依赖库：
```bash
pip install opencv-python numpy pyrealsense2 pyorbbecsdk
```

## 命令行参数

### 必需参数（顺序参数）

| 参数位置 | 参数名 | 说明 | 可选值 |
|---------|--------|------|--------|
| 1 | 容器编号 | 容器的类型编号 | 1-13 |
| 2 | 异常类型 | 异常类型编号 | 1-7 |
| 3 | 小类 | 异常小类文件夹名 | 见下方说明 |
| 4 | 拍摄点位 | 拍摄位置名称 | 如 mixer1, stack1 |
| 5 | 视角编号 | 视角序号，决定文件夹名 | 正整数 |
| 6 | 照片编号 | 文件夹内的第几张照片 | 正整数 |

### 可选参数

| 参数 | 说明 |
|------|------|
| `--list-cameras` | 列出所有已连接的相机信息 |
| `--list-types` | 列出所有容器类型和异常类型信息 |

### 小类参数说明

- **正常类（类型1）**：必须使用 `-` 作为小类参数
- **异常类（类型2-7）**：必须指定具体小类名称

### 容器类型列表

| 编号 | 容器名称 |
|------|---------|
| 1 | beaker（烧杯） |
| 2 | test tube（试管） |
| 3 | test tube modle2（试管模型2） |
| 4 | 6-well plate（6孔板） |
| 5 | 11-well plate（11孔板） |
| 6 | 24-well plate（24孔板） |
| 7 | 48-well plate（48孔板） |
| 8 | 96-well plate modle1（96孔板模型1） |
| 9 | 96-well plate modle 2（96孔板模型2） |
| 10 | magnetic mixer 1（磁力搅拌器1） |
| 11 | magnetic mixer 2（磁力搅拌器2） |
| 12 | rewservoir（储液槽） |
| 13 | 超声波清洗机器 |

### 异常类型列表

| 编号 | 类型名称 | 可用小类 |
|------|---------|---------|
| 1 | 正常 | 无（使用 `-`） |
| 2 | 主体破损 | crack, scratch, wear |
| 3 | 粉末残留 | bottom-powder, crystal, wall-powder |
| 4 | 液体残留 | color, dip, non-color |
| 5 | 污渍 | color dirt, water dirt |
| 6 | object residue（物体残留） | glass rod |
| 7 | tag（标签异常） | damaged, dirty, fall |

#### 孔板和储液槽
1: "正常"
2: "污渍"||"水渍", "颜料污渍"
3: "破损"||"划痕", "裂痕"
4: "液体残留"||"无色液体", "带颜色透明液体", "浑浊液体", "杯壁液体"
5: "固体残留"||"粉末", "结晶"
6: "盖子异常"||"盖子裂痕", "盖子盖错", "没有盖子"
7: "标签异常"||"标签脏污", "标签脱落", "标签破损"
8: "摆放错误"||"斜放"

## 目录结构

脚本会自动创建以下目录结构：

```
数据集根目录/
├── beaker/                    # 容器类型
│   ├── 主体破损/              # 异常类型
│   │   ├── crack/             # 小类
│   │   │   └── mixer1-001/    # 拍摄点位-视角编号
│   │   │       ├── 001_Color.png          # RealSense图像
│   │   │       └── top1/                  # Orbbec图像目录
│   │   │           ├── c1_Color.png       # Orbbec c1
│   │   │           └── c2_Color.png       # Orbbec c2
│   │   └── scratch/
│   │       └── mixer1-002/
│   │           ├── 001_Color.png
│   │           └── top1/
│   └── 正常/                  # 正常类型无小类目录
│       └── mixer1-001/
│           ├── 001_Color.png
│           └── top1/
└── test tube/                 # 其他容器类型
    └── ...
```

## 使用示例

### 1. 查看相机列表
```bash
python capture.py --list-cameras
```

### 2. 查看类型列表
```bash
python capture.py --list-types
```

### 3. 采集正常样本
```bash
# beaker/正常/mixer1-001/ 下第1张照片
python capture.py 1 1 - mixer1 1 1
```

### 4. 采集异常样本（主体破损-裂纹）
```bash
# beaker/主体破损/crack/mixer1-001/ 下第1张照片
python capture.py 1 2 crack mixer1 1 1
```

### 5. 同一视角多张照片
```bash
# 第2张照片，保存在同一目录
python capture.py 1 2 crack mixer1 1 2
```

### 6. 不同视角采集
```bash
# 换视角到 mixer1-002
python capture.py 1 2 crack mixer1 2 1
```

## 输出说明

### 成功输出
- RealSense 图像保存为 `{照片编号}_Color.png`
- Orbbec 图像保存在 `top{照片编号}/` 目录下
- c1 相机（序列号 CL8K14100H4）保存为 `c1_Color.png`
- c2 相机（另一台 Orbbec）保存为 `c2_Color.png`

### 控制台输出示例
```
保存目录: /home/qy/dataset-202607/quality test/empty container/beaker/主体破损/crack/mixer1-001
RealSense -> /home/qy/.../001_Color.png
Orbbec[0] SN=CL8K14100H4 -> c1
Orbbec[1] SN=CL8K14100H3 -> c2
Orbbec c1 (SN=CL8K14100H4) -> .../top1/c1_Color.png
Orbbec c2 (SN=CL8K14100H3) -> .../top1/c2_Color.png
采集完成
```

## 错误处理

### 常见错误及解决方案

1. **缺少参数**
   ```
   错误：缺少参数: container, anomaly_type
   解决：检查是否提供了所有6个必需参数
   ```

2. **相机未找到**
   ```
   错误：需要 2 台 Orbbec 相机，当前检测到 1 台
   解决：检查相机连接状态
   ```

3. **c1 相机未找到**
   ```
   错误：未找到 c1 相机 (SN=CL8K14100H4)
   解决：确认序列号为 CL8K14100H4 的 Orbbec 相机已连接
   ```

4. **小类参数错误**
   ```
   错误：异常类型 2 (主体破损) 必须指定小类文件夹名，可选: crack, scratch, wear
   解决：为异常类型提供正确的小类名称
   ```

5. **正常类小类错误**
   ```
   错误：正常类不需要小类，小类参数请填 -
   解决：正常类使用 - 作为小类参数
   ```

## 注意事项

1. **相机预热**：脚本会自动丢弃前 15 帧（WARMUP_FRAMES）以确保图像稳定

2. **相机编号**：Orbbec c1 相机序列号固定为 `CL8K14100H4`，如需更改请修改脚本中的 `ORBBEC_C1_SERIAL` 变量

3. **数据集根目录**：默认保存在 `/home/qy/dataset-202607/quality test/empty container`，如需修改请更新 `DATASET_ROOT` 变量

4. **权限要求**：确保有足够的权限读写目标目录

5. **相机连接**：采集过程中请确保所有相机稳定连接，不要断开

## 技术细节

### 图像格式支持
- RealSense：自动使用默认彩色格式
- Orbbec：支持 RGB、BGR、YUYV、UYVY、MJPG 等多种格式，会自动转换

### 采集流程
1. RealSense 相机启动并预热
2. 采集 RealSense 彩色图像
3. Orbbec 相机启动并预热
4. 分别采集两台 Orbbec 彩色图像
5. 所有图像保存到指定目录

## 维护与自定义

如需添加新的容器类型或异常类型，可以修改脚本中的以下字典：
- `CONTAINERS`：容器类型映射
- `ANOMALY_TYPES`：异常类型映射
- `ANOMALY_SUBCATEGORIES`：异常小类列表

## 故障排除

### 如果采集失败
1. 检查相机连接状态
2. 确认相机驱动已正确安装
3. 检查是否有其他进程占用相机
4. 验证目标目录是否存在且可写
5. 查看控制台输出的详细错误信息

### 如果图像格式不正确
- Orbbec 相机支持多种格式，脚本会自动处理
- 如遇到不支持的格式，会在控制台输出警告信息